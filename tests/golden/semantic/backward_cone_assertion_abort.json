{
  "name": "backward cone indexes concurrent assertion abort sources without a fallback",
  "sources": [
    {
      "uri": "file:///golden/assertion/abort.sv",
      "text": "module top(input logic clk, input logic abort_now, input logic a, input logic b);\n  assert property (@(posedge clk) accept_on(abort_now) a |-> b);\nendmodule\n"
    }
  ],
  "request": { "kind": "backwardCone", "uri": "file:///golden/assertion/abort.sv", "line": 1, "character": 2 },
  "expected": {
    "edgeKinds": ["assertionSample"],
    "sourceRoles": ["sampled", "clock", "abort"],
    "controlOrigins": ["assertionClock", "assertionAbort"],
    "expressions": ["clk", "abort_now", "a", "b"]
  }
}
