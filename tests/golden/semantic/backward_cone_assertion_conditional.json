{
  "name": "backward cone retains assertion conditional guards as typed controls",
  "sources": [
    {
      "uri": "file:///golden/assertion/conditional.sv",
      "text": "module top(input logic clk, input logic choose, input logic a, input logic b);\n  assert property (@(posedge clk) if (choose) a else b);\nendmodule\n"
    }
  ],
  "request": { "kind": "backwardCone", "uri": "file:///golden/assertion/conditional.sv", "line": 1, "character": 2 },
  "expected": {
    "edgeKinds": ["assertionSample"],
    "sourceRoles": ["sampled", "clock", "control"],
    "controlOrigins": ["assertionClock", "assertionConditional"],
    "expressions": ["clk", "choose", "a", "b"],
    "assertionTemporalEdgeCount": 3,
    "temporalRelations": ["conditionalBranch"]
  }
}
